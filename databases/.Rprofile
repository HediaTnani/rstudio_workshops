# source("renv/activate.R")
options("plumber.port" = 5762)

# Set environment variables for username and password
# Use Sys.getenv("<var-name>") to get back
Sys.setenv(
  db_uid = "rstudio_prod", 
  db_pass = "prod_user"
)
